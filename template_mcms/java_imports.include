<#assign className = table.className>   
<#assign classNameLowerCase = className?lower_case>  
import ${basepackage}.dmo.${namespace1}.<#if namespace2??>${namespace2}.</#if>${className}Entity;

import java.util.List;
